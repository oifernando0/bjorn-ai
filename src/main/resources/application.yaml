spring:
  application:
    name: bjorn-ai
  datasource:
    url: jdbc:postgresql://localhost:5432/bjorn_db
    username: bjorn
    password: bjorn123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8080

bjorn:
  storage:
    dir: ${BJORN_STORAGE_DIR:./storage}
